# 2026-07 변경 이력

## 2026-07-02 프로젝트 변경 - 시스터 주문서 운영 export API 사용

### 요청 원문

> order-worker 주문서 자동 등록에서.. sister 는 에러나서 주문서 등록이 안되는데. .현재 시스터>조원 에 주문서가 있으니까..등록 되도록 수정해줘.
>
> HTTPConnectionPool(host='localhost', port=3000): Max retries exceeded with url: /api/seller/orders/export (Caused by NewConnectionError("HTTPConnection(host='localhost', port=3000): Failed to establish a new connection: [WinError 10061] 대상 컴퓨터에서 연결을 거부했으므로 연결하지 못했습니다"))

### 결과

시스터 주문서 export 기본 URL이 로컬 개발 서버 `http://localhost:3000/api/seller/orders/export`로 되어 있어, 로컬 시스터 서버가 꺼져 있으면 주문서 다운로드가 실패했다. 기본 URL을 운영 시스터 API `https://prahashop.co.kr/api/seller/orders/export`로 변경해 order-worker가 현재 시스터 조원 주문서를 운영 서버에서 직접 다운로드하도록 수정했다. `.env.example`에도 같은 URL을 명시했다.

### 변경 파일

- `order_worker/config.py`
- `.env.example`
- `docs/changes/2026-07.md`

### 검증

- `.\.venv\Scripts\python.exe -m order_worker.main run --site sister` 성공
- 시스터 주문서 다운로드 성공
- 인트라넷 주문 등록 결과: inserted 8, duplicate 0

### 커밋

- `aafcdf6` Use production Sister order export API
- `07a97c6` Record Sister order export API change

## 2026-07-03 프로젝트 변경 - 시스터 송장 업로드 API www 도메인 직접 사용

### 요청 원문

> 인트라넷 > Add Data > 송장 자동 업로드하는데.. 시스터는  에러나..
> 확인해줘.  시스터: 시스터 송장 업로드 실패 (500): HTTP 500

### 결과

시스터 송장 업로드 기본 API가 `prahashop.co.kr`로 되어 있어 운영에서 `www.prahashop.co.kr`로 307 리다이렉트되는 것을 확인했다. 자동 업로드 요청의 변수를 줄이기 위해 기본 업로드 URL을 `https://www.prahashop.co.kr/api/seller/invoice-upload`로 직접 변경했다.

### 변경 파일

- `order_worker/config.py`
- `docs/changes/2026-07.md`

### 검증

- `python -m py_compile order_worker/config.py order_worker/sites/sister_invoice.py` 통과
- `git diff --check` 통과

### 커밋

- `f381a69` Use www Sister invoice upload API
- `c4a16d1` Record Sister invoice upload URL change

## 2026-07-03 프로젝트 변경 - 주문서 없음과 실패 원인 표시 개선

### 요청 원문

> order-worker 어제 07a97c6 이거 수정한뒤부터인거 같은데.. 이후로 주문서 수집이 안되는거 같음
>
> 알 수 없는 오류라고 모두 뜸.
>
> 그리고 주문서 수집시. 주문서가 하나도  없는 사이트가 있으면 알 수 없는 오류 라고 표시 되는거 같음 / 주문서 없음으로 텔레그램 내역 보내주도록 수정해줘.

### 결과

인트라넷 주문 import 응답의 상세 실패 사유가 `errors[]`에만 들어오는 경우 order-worker가 이를 읽지 못해 텔레그램에 `알 수 없는 오류`로 표시되던 문제를 수정했다. 또한 `데이터 행이 없습니다` 같은 빈 주문서 응답은 실패가 아니라 `주문서 없음`으로 정규화해 콘솔과 텔레그램에 정상 상태로 표시되도록 했다.

조사 중 전체 주문 수집 실패의 직접 원인은 인트라넷 API가 오래 실행된 Prisma Client를 사용해 `intra_product_options.is_hidden` 필드를 모르는 상태였던 것으로 확인했다. 인트라넷 Prisma Client를 재생성하고 로컬 3001 서버를 재시작해 새 스키마를 반영했다.

### 변경 파일

- `order_worker/main.py`
- `order_worker/notifier.py`
- `order_worker/sites/utils.py`
- `docs/changes/2026-07.md`

### 검증

- `python -m py_compile order_worker\main.py order_worker\notifier.py order_worker\sites\utils.py` 통과
- `.\.venv\Scripts\python.exe`로 주문 없음 응답 정규화 확인
- `.\.venv\Scripts\python.exe`로 `errors[]` 실패 원인 텔레그램 요약 반영 확인
- 인트라넷 `npx prisma generate` 실행 후 `node_modules\.prisma\client\schema.prisma`의 `is_hidden` 반영 확인
- 인트라넷 로컬 서버 `http://localhost:3001` 재시작 및 `/api/order-import` 라우트 응답 확인

### 커밋

- `b711a8e` Handle empty order imports in worker

## 2026-07-08 프로젝트 변경 - Railway order-worker 배포 설정 추가

### 요청 원문

> 좋아.그러면 지금 order-worker  가 하는걸 그데로 레일웨이에 올려서 할수있도록 해줘.
> 주문서 수집, 송장번호 업데이트 등..
>
> 내가 미리 해줘야할꺼 있으면 얘기해주고
>
> 레일웨이에 프로젝트 생성 해야하지?
>
> 뭐로 만들까?

### 결과

order-worker를 Railway GitHub Repository 서비스/Cron으로 배포할 수 있도록 Docker 기반 배포 설정을 추가했다. Playwright Chromium이 포함된 공식 Python 이미지를 사용하도록 `Dockerfile`을 만들고, Railway가 Dockerfile 빌드를 사용하도록 `railway.json`을 추가했다. 같은 이미지에서 `ORDER_WORKER_TASK=collect`이면 주문서 수집, `ORDER_WORKER_TASK=invoices`이면 송장번호 업로드가 실행되도록 Railway 엔트리포인트를 추가했다.

Railway 런타임에서는 다운로드/아카이브/로그/락 파일이 기본적으로 `/tmp/order-worker` 아래에 생성되도록 config 기본 경로를 조정했다. 장기 파일 보관이 필요하면 Railway Volume을 붙이고 `ORDER_WORKER_RUNTIME_DIR=/data/order-worker`로 바꾸면 된다.

### 변경 파일

- `.dockerignore`
- `.env.example`
- `.gitattributes`
- `Dockerfile`
- `docs/railway.md`
- `order_worker/config.py`
- `pyproject.toml`
- `railway.json`
- `scripts/railway-entrypoint.sh`
- `docs/changes/2026-07.md`

### 검증

- `python -m compileall -q order_worker` 통과
- `.\.venv\Scripts\python.exe -m compileall -q order_worker` 통과
- `.\.venv\Scripts\python.exe -m unittest discover -s tests` 통과, 7 tests OK
- `python -m json.tool railway.json` 통과
- `.\.venv\Scripts\python.exe -m order_worker.main sites` 통과
- `.\.venv\Scripts\python.exe -m order_worker.main invoice-sites` 통과
- `git diff --cached --check` 통과
- 로컬 PC에 Docker CLI가 없어 로컬 Docker image build는 미실행

### 커밋

- `aa46308` Add Railway deployment for order worker
