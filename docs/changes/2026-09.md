# 2026-09 변경 이력

## 2026-09-01 오너클랜 상품일괄등록 자동화

- 인트라넷 상품등록 작업 payload를 받아 오너클랜과 F오너클랜의 상품 개별등록 화면을 자동 입력하는 작업 유형을 추가했다.
- 등록 전 모델명으로 중복 상품을 조회하고, 이미 등록된 상품은 성공 상태로 건너뛴다.
- 오너클랜은 G코드 상품명/모델명과 제조사 `조원`, F오너클랜은 F코드 상품명/모델명과 제조사 `프라하`를 사용한다.
- 패션의류 > 여성의류 > 스커트, 해외 > 아시아 > 중국, 과세, 색상/사이즈 옵션, 대표·추가 이미지, 상세 HTML, 선불 3,000원, 원운송장 반품접수, 묶음배송 100개, 인증대상 아님, 상품정보고시 전체 별도표기를 자동 적용한다.
- 오너클랜 최초 지적재산권 경고 확인 뒤 즉시 발생하는 페이지 이동을 최종 모델명 재조회로 판정하도록 처리했다.
- 실상품 `G127151`/`F007780`으로 최종 제출 전 자동입력을 각각 검증했고, 실제 등록 후 오너클랜 상품코드 `WFQIJDN`, F오너클랜 상품코드 `WFQIJJ0`가 승인대기로 생성된 것을 확인했다.

### 검증

- `python -m py_compile order_worker/sites/ownerclan_registration.py order_worker/main.py`
- `python -m pytest -q` (54 passed)
- G/F 계정 최종 제출 전 자동입력 검증 성공
- G/F 계정 등록 후 상품관리 모델명, 상품명, 제조사, 판매가 재조회 성공

## 2026-09-01 온채널/F온채널 상품 임시등록 자동화

- 상품일괄등록 작업을 사이트별 실행기로 분리해 기존 오너클랜 등록과 온채널 임시저장을 한 작업에서 순서대로 처리한다.
- 온채널 공급사 `상품 등록 > 개별 등록 > 신규 등록` 화면에서 제조사·가격자율·주의사항 확인 뒤 카테고리, 상품명, 키워드, 과세, 미성년자 제한, 공급사코드, 쿠팡 전송, 배송/주소/묶음배송, 상품정보고시, 상세 HTML, 대표 이미지 4장, 옵션별 공급가를 자동 입력한다.
- 카테고리 추천이 표시되면 사용자가 정한 카테고리를 유지하고, 승인 요청은 하지 않은 채 `임시 저장`까지만 실행한다.
- 등록중/등록완료 목록을 먼저 조회해 같은 상품코드의 중복 임시저장 및 중복 등록을 방지한다.
- 실상품 `G127151`과 `F007780`을 각각 공급가 23,000원으로 임시저장했으며 온채널 내부코드 `CH5353251`, `CH5353252`를 확인했다.
- 수정 화면 재검증에서 두 상품 모두 `패션의류 > 여성의류 > 스커트`, 과세, 미성년자 판매 가능, `호피스커트/FREE`, 공급가 23,000원, 남천로 31 출고/반품 주소와 계정별 A/S 정보가 저장된 것을 확인했다.

### 검증

- `python -m py_compile order_worker/sites/onchannel_registration.py order_worker/sites/product_registration.py order_worker/main.py`
- `python -m pytest -q` (56 passed)
- G/F 계정 임시저장 전 자동입력 검증 성공
- G/F 계정 등록중 목록 및 수정 화면 재조회 성공

## 2026-09-02 온채널 상품상세 HTML 및 계정별 대표이미지 수정

- 온채널이 사용하는 CKEditor 5의 `Source` 버튼을 실제로 클릭하고, 표시되는 소스 입력 영역에 상품상세 HTML을 입력하도록 변경했다.
- 입력 직후 소스 영역의 값이 요청 HTML과 같은지 검증하고, 보이는 임시 저장 버튼만 선택하도록 안전장치를 추가했다.
- 실상품 `G127151`과 `F007780`의 기존 임시저장 상품을 수정해 상세 이미지 URL 19개를 각각 저장했다.
- 온채널 대표이미지는 `G127151_1000_1.jpg`, F온채널 대표이미지는 `G127151_1000_4.jpg`로 교체했다.

### 검증

- `python -m unittest discover -s tests -v` (52 passed)
- 수정 화면 Source 재조회: 두 계정 모두 상세 이미지 URL 19개 저장 확인
- 저장된 온채널 썸네일과 `_1000_1` 원본의 픽셀 일치 확인
- 저장된 F온채널 썸네일과 `_1000_4` 원본의 픽셀 일치 확인

## 2026-09-02 오너클랜 품절 처리 탐색 타임아웃 수정

- 오너클랜/F오너클랜 상품관리 페이지가 화면 준비 후에도 광고·추적 연결을 유지해 `networkidle`에 도달하지 못하면서 30초 타임아웃이 발생한 원인을 확인했다.
- 로그인, 상품관리, 검색 결과, 상품 수정 팝업과 옵션 팝업 이동을 `domcontentloaded`와 실제 필수 DOM 기준으로 변경했다.
- 필요한 화면이 준비된 뒤 `window.stop()`으로 불필요한 보조 리소스 로딩을 중단해 동일한 탐색 타임아웃이 재발하지 않도록 했다.
- 실패했던 `G127104`와 `F007733`을 오너클랜/F오너클랜에서 다시 품절 처리하고 저장 상태를 재조회해 성공을 확인했다.
- 인트라넷의 기존 부분 실패 결과도 두 계정의 재처리 성공을 합쳐 `succeeded` 및 상품 품절 상태로 정리했다.

### 검증

- `python -m pytest -q` (59 passed)
- 오너클랜 `G127104` 상품 품절 처리 및 재조회 성공
- F오너클랜 `F007733` 상품 품절 처리 및 재조회 성공
- 인트라넷 `G127104` 상태: `external_soldout=true`, `external_status=succeeded`

## 2026-09-02 스페셜오퍼 상품일괄등록 자동화

- 인트라넷 상품일괄등록 작업에서 스페셜오퍼 상품등록 화면을 자동 입력하고 최종 저장하는 실행기를 추가했다.
- `여성의류 > 스커트 > 롱 스커트`, 업체상품코드/상품명/키워드, 프라하 브랜드·제조사, 해외 > 아시아 > 중국, 14:00 발주마감, 색상·사이즈 조합 옵션을 자동 적용한다.
- 소비자가는 B2B 공급가의 1.8배, 공급가격은 B2B 가격으로 입력하고, 유료배송·선결제·기본배송비 3,000원과 지역별 추가배송 안내를 적용한다.
- 참고이미지를 최대 6장까지 업로드하고 SmartEditor의 HTML 모드를 실제로 선택해 상세이미지와 사이즈이미지 HTML을 입력한다.
- 공급가 입력 시 사이트가 소비자가를 자동 재계산하는 동작을 확인해 소비자가를 마지막에 확정하고 저장 직전 세 금액을 다시 검증하도록 했다.
- 실상품 `G127151`을 등록하고 수정 화면에서 소비자가 41,400원, 공급가격 23,000원, 이미지 4장, 상세 이미지 URL 19개, 옵션 `호피스커트/free`가 저장된 것을 확인했다.

### 검증

- `python -m unittest discover -s tests -v`
- `python -m py_compile order_worker/sites/specialoffer_registration.py order_worker/sites/product_registration.py`
- G127151 저장 전 자동입력 검증 성공
- G127151 전체상품 관리 검색 및 수정 화면 저장값 재조회 성공

## 2026-09-02 도매의신 상품일괄등록 자동화

- 인트라넷 상품일괄등록 작업에 도매의신 상품등록 실행기를 추가했다.
- 상품별 카테고리, 업체상품코드, 해외 > 아시아 > 중국 원산지, 프라하 브랜드·제조사, 모델명과 키워드를 자동 입력한다.
- 판매가는 도매꾹 판매금액, 소비자가는 해당 판매가의 1.8배로 계산하며 기본배송비 3,000원과 반품 편도배송비 3,000원을 적용한다.
- 색상×사이즈 조합별 선택옵션 행을 직접 만들고, 대표이미지를 업로드하며 CKEditor의 실제 소스 버튼을 클릭해 상세·사이즈 이미지 HTML을 입력한다.
- 상품정보고시는 상품 종류에 맞는 항목을 선택하고 모든 세부 항목을 `상품 상세설명에 표시`로 저장한다.
- 도매의신이 상품명 밑줄을 허용하지 않는 제약을 확인해 밑줄만 공백으로 자동 변환한다.
- 실상품 `G127151`을 등록하고 판매가 25,300원, 소비자가 45,540원, 기본배송 3,000원, 반품배송비 3,000원, 옵션 `호피스커트/free`, 대표이미지와 상세 이미지 19개 저장을 재조회했다.
- 기본배송 정책에서는 사이트가 묶음배송수량 입력란을 비활성화하고 저장값을 0으로 관리하며, 기본배송 자체는 묶음배송비(높은 금액 적용) 정책으로 동작함을 확인했다.

### 검증

- `python -m unittest discover -s tests -v`
- `python -m py_compile order_worker/sites/domesin_registration.py order_worker/sites/product_registration.py`
- G127151 저장 전 자동입력 검증 성공
- G127151 상품관리 및 수정 화면 저장값 재조회 성공

## 2026-09-03 남도마켓 상품일괄등록 자동화

- 인트라넷 상품일괄등록 작업에 남도마켓 상품등록 실행기를 추가했다.
- 로그인 뒤 건물정보 안내를 닫고, 등록 전 관리코드 검색으로 중복 상품 생성을 방지한다.
- 상품별 참고이미지를 최대 20장까지 첨부하고 첫 이미지를 대표컷으로 사용하며, 별도의 상세 이미지에는 메인컷 1장을 첨부한다.
- 카테고리, 색상, 사이즈, 도매 판매가, 폴리에스테르 소재, 중국 제조국, 고정 상품설명, 인트라넷 키워드 태그와 전체공개를 자동 적용한다.
- 남도마켓 로그인 정보는 코드에 넣지 않고 Railway의 `NAMDO_USER_ID`, `NAMDO_PASSWORD` 비밀 환경변수로만 제공한다.
- 실상품 `G127151`을 참고이미지 4장, `여성의류 > 스커트 > 미디·롱스커트`, `호피스커트/free`, 판매가 19,000원으로 등록하고 상품관리 재검색을 통과했다.

### 검증

- `python -m compileall order_worker/sites/namdo_registration.py order_worker/sites/product_registration.py`
- `python -m unittest discover -s tests -p 'test_product_registration.py'` (2 passed)
- G127151 저장 전 자동입력 검증 성공
- G127151 최종 등록 및 상품관리 관리코드 재조회 성공

## 2026-09-03 남도마켓 size 이미지 규격 보정 및 기존 상품 수정

- 남도마켓 상품이미지 등록 순서에 포함된 비정사각형 size 이미지가 `등록불가`로 차단되는 현상을 확인했다.
- 상품이미지를 전송하기 전에 원본 비율과 전체 내용을 유지한 채 흰 여백을 넣어 1000×1000 JPEG로 자동 변환하도록 등록 작업기를 수정했다.
- 대표컷, image1 상품사진, 마지막 size 이미지를 포함한 모든 남도마켓 상품이미지에 동일한 규격 보정을 적용한다.
- 실상품 G127151의 기존 등록분에도 정사각형으로 보정한 size 이미지를 마지막에 추가해 16/20장으로 저장했다.

### 변경 파일

- `order_worker/sites/namdo_registration.py`
- `pyproject.toml`
- `tests/test_namdo_registration.py`
- `docs/changes/2026-09.md`

### 검증

- `python -m pytest -q` (63 passed)
- 수정 파일 `py_compile` 통과
- 남도마켓 원본 800×472 size 이미지의 `등록불가` 재현
- 1000×1000 보정본 업로드 시 오류 없음
- G127151 수정 저장 후 이미지 16장 및 `등록불가` 없음 재조회
- 구현 커밋: `c199c22`

### 기록 정보

- 프로젝트: order-worker
- 완료 시각: 2026-09-03 KST
- 작성자: Codex
- 프로젝트 변경 이력: `#389`
